#include <iostream>
#include <cmath>
#include <vector>

using namespace std;

void hanoiTree(int n, int a, int b)
{
    //int Tr[100];
    vector<int> Tr(100);

    int first,last,sh,i,j,h,f,t;

    first = 1 << (n-1);
    last = first;
    Tr[first].h
}

int main()
{
    int fi = 10;
    int n = 1;
    fi = 1 << (n-1);
    cout<<fi;
    return 0;
}













